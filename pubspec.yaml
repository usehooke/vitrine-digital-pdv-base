name: vitrine_nova
description: "A new Flutter project."
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter

  # Firebase
  firebase_core: ^2.32.0
  firebase_auth: ^4.16.0
  cloud_firestore: ^4.17.5
  flutter_gemini: 3.0.0

  js: ^0.6.7

  firebase_storage: ^11.0.16

  # Roteamento
  go_router: ^14.2.0

  # Gestão de Estado e Injeção de Dependência
  provider: ^6.1.2

  # Ferramentas de UI e Dispositivo
  image_picker: ^1.1.2
  qr_flutter: ^4.1.0
  uuid: ^4.2.0
  cached_network_image: ^3.2.0

  
  # Ícones
  cupertino_icons: ^1.0.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  mockito: ^5.4.0
  build_runner: ^2.4.6

flutter:
  uses-material-design: true