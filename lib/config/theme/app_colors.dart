// lib/config/theme/app_colors.dart
import 'package:flutter/material.dart';

class AppColors {
  AppColors._(); // Construtor privado

  // Cores da sua paleta
  static const Color primaryBlue = Color(0xFF0ea5e9);
  static const Color slateDark = Color(0xFF0f172a);
  static const Color slateLight = Color(0xFF1e293b);
  static const Color slateGray = Color(0xFF334155);
}